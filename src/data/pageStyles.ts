// The class lists every marketing page shares. One home, so a spacing tweak lands
// everywhere at once.
export const EYEBROW =
  'eyebrow m-0 text-xs leading-relaxed font-semibold tracking-[0.18em] text-muted uppercase';

// Text lines up with the header's wordmark; see the note in index.astro.
export const TEXT_COL = 'w-full max-w-[61rem]';

export const CARD = 'rounded-xl border border-border bg-panel px-5 py-4.5';

// The two display sizes the section components set headings in. Pages that need a
// different scale (the home hero) pass their own class instead.
export const DISPLAY_LG =
  'text-[clamp(2.2rem,5.5vw,3.5rem)] leading-[1.06] font-extrabold tracking-tight';
export const DISPLAY_MD =
  'text-[clamp(1.9rem,4.2vw,3rem)] leading-[1.08] font-extrabold tracking-tight';

// The screenshot chrome every marketing picture and video shares.
export const SHOT_FRAME = 'block h-auto w-full rounded-xl border border-border';
